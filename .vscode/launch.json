{
  "configurations": [
    {
      "type": "gdb",
      "request": "launch",
      "name": "Launch Program",
      "target": "${workspaceRoot}/build/controlxlinux",
      "cwd": "${workspaceRoot}/build",
      "valuesFormatting": "parseText",
      "arguments": "nspire-cx.dtb zImage.tns initrd.tns controlxlinux.img.tns"
    }
  ]
}
